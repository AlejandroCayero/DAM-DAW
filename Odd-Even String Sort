public class OddEvenSort {
  public static String sortMyString(String s) {
    String pares ="";
    String impares = "";
    for(int i =0;i<s.length();i++){
      if(i%2==0){
        pares+=s.charAt(i);
      }else{impares+=s.charAt(i);}
    }
    
    return pares+" "+impares;
  }
}
